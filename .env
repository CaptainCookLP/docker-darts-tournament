# Autodarts Tournament Server
# HTTP
PORT=8080

# HTTPS/WSS
TLS_ENABLED=false
TLS_PORT=8443
TLS_KEY_PATH=/certs/privkey.pem
TLS_CERT_PATH=/certs/fullchain.pem

DB_PATH=/data/tournament.sqlite
CORS_ORIGINS=*

# TLS (HTTPS/WSS) — set TLS_ENABLED=true and mount certs into /certs
TLS_ENABLED=false
TLS_KEY_FILE=/certs/privkey.pem
TLS_CERT_FILE=/certs/fullchain.pem
